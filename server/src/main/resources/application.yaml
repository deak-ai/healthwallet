ktor:
  deployment:
    port: "$HTTP_PORT:8080"
  application:
    modules:
      - ch.healthwallet.web.ApplicationKt.module
services:
  issuer: "https://issuer.healthwallet.li"
  verifier: "https://verifier.healthwallet.li"
db:
  url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;"
  driver: "org.h2.Driver"
  user: "nouser"
  password: "nopassword"



